<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>Adyen Skinbuilder</title>
    <style>
      body {
        margin: 20px;
        padding: 0px;
        border: 0px;
        background-color: #CECECE;
        font-size: 0.8em;
        font-family: Verdana, Geneva, Arial, Helvetica, sans-serif;
      }

      h1 {
        color: #F0AB00;
        font-size: 1.8em;
        font-family: "Trebuchet MS", Arial, sans-serif;
      }

      .content {
        min-height: 480px;
        background: #FFFFFF;
      }

      .top {
        height: 15px; /* 95px */
        background: #3C8A2E;
      }

      .skins {
        padding-left: 150px;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <div class="top"></div>
      <div class="skins">
        <h1>Adyen Skins</h1>

        Skins found in <i><%= skin_path %></i>

        <table>
          <tr>
             <th>Code</th>
             <th>Name</th>
             <th>Menu</th>
          </tr>
          <% @skins.each do |skin| %>
            <tr>
              <td><a href="/<%= File.basename(skin.path.to_s) %>"><%= skin.code %></a></td>
              <td><a href="/<%= File.basename(skin.path.to_s) %>"><%= skin.name %></a></td>
              <td>
                 <a href="/<%= File.basename(skin.path.to_s) %>?upload=true">upload</a>
                 <a href="https://ca-test.adyen.com/ca/ca/skin/testpayment.shtml?skinCode=<%= skin.code %>" target="_blank">test</a>
              </td>
            </tr>
          <% end %>
        </ul>
      </div>
    </div>

  </body>
</html>
